# NEXT SESSION GUIDE (260418)

## 🚨 [PRIORITY 0] 신규 테마 포스팅 집필 (주제 대기 중)
**현재 도메인 이사가 완료된 직후, 새로운 브랜딩(`tikklabs.com`)으로 발행할 첫 글입니다.**
- **내용**: 대장이 제시할 새로운 주제에 기반하여 아서 G. 페르소나로 집필.
- **배경**: `tikklabs.com`의 신선한 활력을 구글 봇에게 인식시키기 위한 전략적 포스팅.

## 🚨 [PRIORITY 0.1] 프로젝트 보안 및 아키텍처 고도화 (젬웅 지시사항)
- **현상태**: 예약됨 (도메인 이슈 해결 후 즉시 실행 대기)
- **내용**: 공개 레포(rohbomb.github.io)의 원본 소스 노출 차단 및 비공개 레포(tikklab) 분리.

## 🚨 [PRIORITY 2] 3040 '디지털 부양' 가이드 포스트 개편 (Manifesto & Roadmap)
- **현상태**: **잠정 보류** (대장 지시사항으로 신규 포스트 먼저 진행)
- 공개 레포(rohbomb.github.io)의 원본 소스 노출 차단 및 비공개 레포(tikklab) 분리.
### 1. 목적
- 공개 레포(rohbomb.github.io)의 원본 Markdown 및 설정 파일 노출 차단.
- 비공개 레포(tikklab)를 '소스/빌드 공장'으로, 공개 레포를 '정적 결과물 쇼룸'으로 완전 분리.

### 2. 사전 상태 파악 (필수 보고)
- 시작 전 `tikklab` 레포의 `deploy.yml` 전체 내용/구조 분석 보고.
- 공개 레포의 불필요한 파일(config, themes, content 등) 리스트업.

### 3. 상세 요건
- **전용 빌드 워크플로우**: `tikklab` 빌드 결과물(`public/`)만 공개 레포로 Push.
- **SEO 보존**: `CNAME`, `sitemap.xml`, `robots.txt` 유실 금지.
- **제약**: CSS `!important` 금지, 하드코딩 지양, 수정 전 Line count 보고.

---

# 세션 인수인계 보고서 (260313 1700)

## [현재 진행 상태]
- **글로벌 다국어 개편 완료**: 영어(`en`) 기본 주소 및 한국어(`ko`) 서브 디렉토리 구조 확립.
- **디자인 정공법 이식**: 레이아웃 오버라이드를 통해 `#` 앵커 원천 제거 및 CSS `!important` 의존도 감소.
- **디자인 조리법(Cookbook)**: 대장이 직접 수정 가능하도록 `custom.css`에 상세 가이드 주석 추가 완료.
- **빌드 복구**: `desktop.ini` 완전 제거 및 `.gitignore` 등록으로 GitHub Actions 초록불 유지 중.

## [미해결 이슈]
- 특이사항 없음. 모든 로컬 변경 사항이 깃허브 공개단/비공개단에 완벽하게 동기화됨.

## [다음 세션 첫 액션]
- **글로벌 타겟 포스팅**: `content/en/post/`에 영문으로 된 고품질 IT 분석글 첫 발행.
- **수동 디자인 동기화**: 대장이 깃허브 웹에서 직접 수정한 색상이 있다면 `git pull`로 로컬에 동기화 후 작업 재개.

## [헌법/로직 업데이트 사항]
- **다국어 경로**: 앞으로 한국어 포스팅은 무조건 `content/ko/post/` 하위에 생성할 것.
- **전역 헌법 인지**: `GEMINI.md`는 시스템 내장 지침으로 인지할 것. 물리적 파일은 워크스페이스 외부에 있으므로 **보안상 접근 시도는 절대 금지**함.

---
*(Edited by 🐻 Win-Woong)*
